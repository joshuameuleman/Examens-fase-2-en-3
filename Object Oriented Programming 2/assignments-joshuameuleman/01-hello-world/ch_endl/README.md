# Syntax `endl`

What does `endl` do? It is often found after outputting some information to the terminal. What is the difference with `"\n"`?

## Answer

endl make your next line go under the previous line and not next to the previous one

why not use \n ? this uses more bytes
\n does not make the next line flush with previous line 
that's why you can have

this is an example with endl.
this is flush because of endl.

this is an example with \n.
   this can make text not flush.