# Squaring a number

How would you square the value in `x` and store it in `squared`? Use only the basic math operators.

```cpp
int x = 12;

int squared;
```

**answer**

```cpp
int x = 12;

int squared=(x*x);
```